{% extends 'baseClient.html.twig' %}

{% block Assurance %} active {% endblock %}

{% block body %}
    <h1>Assurance index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Image</th>
                <th>Partenaire</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for assurance in assurances %}
            <tr>
                <td>{{ assurance.id }}</td>
                <td>{{ assurance.libelle }}</td>
                <td>{{ assurance.image }}</td>
                <td>{{ assurance.partenaire }}</td>
                <td>{{ assurance.type }}</td>
                <td>
                    <a href="{{ path('app_assurance_show_front', {'id': assurance.id}) }}">show</a>
                    <a href="{{ path('app_assurance_edit_front', {'id': assurance.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_assurance_new_front') }}">Create new</a>
{% endblock %}
